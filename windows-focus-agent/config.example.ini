[mqtt]
host = 192.168.1.15
port = 1883
username = ultra-magners
; Password is NOT stored here. Provide it (priority order):
;   1. environment variable MQTT_PASSWORD
;   2. Windows Credential Manager:  python focus_agent.py --set-password
;   3. a plaintext value below (discouraged fallback)
password =
topic = focus/state

[focus]
; method: dnd (default; clicks the real Quick Settings toggle, needs pywinauto)
;       | toasts / focus_assist (legacy; dead on Win11 2026 builds) | command
method = dnd
; focus_assist only: 1 = Priority only, 2 = Alarms only
assist_level = 1
; command only: run your own script/exe on each transition
on_command =
off_command =
