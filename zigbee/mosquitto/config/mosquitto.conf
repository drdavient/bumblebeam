listener 1883
# Anonymous access is acceptable here because the broker is not exposed to the LAN:
# the host port is bound to 127.0.0.1 and Z2M reaches it over the internal zigbee-net.
# Add authentication before this broker becomes a permanent Home Assistant dependency.
allow_anonymous true
persistence false
log_dest stdout
