# Stabilisation task register

| Priority | Task | Status | Dependency / target | Acceptance test | Evidence |
|---|---|---|---|---|---|
| P0 | Review reported Plex token exposure | Deferred | Exposure is unverified; account-wide client reauthentication was declined | Revisit if evidence is substantiated; keep runtime token out of Git | Current checkout contains no Git history or exposed token evidence |
| P0 | Review previously embedded Cloudflare credentials | Deferred | Local embedding was corrected; external exposure is unverified | Keep scoped credentials out of Git; rotate during routine account maintenance if desired | Current checkout contains no Git history or exposed token evidence |
| P0 | Classify repository data | Done | Phase 0 | Inventory covers every service/state class | `docs/inventory.md` |
| P0 | Record baseline | Done | Phase 0 | Container, DNS, HTTP, mount, network, storage checks recorded | `docs/baseline-2026-07-11.md`; `docs/evidence/2026-07-11-implementation.md` |
| P0 | Verify Elements read-write mount | Done | Phase 0 | Expected UUID mounted `rw`; sentinel present | Host `findmnt` and successful repository write |
| P0 | Install Restic | Done | Phase 0 | `restic version` succeeds | Workspace-local Ubuntu 0.12.1 package |
| P0 | Initial consistent local snapshot | Done | Elements rw, Restic, recovery password | `restic check --read-data` and local restore pass | Latest `dddc33cb`; full check and local restore passed 2026-07-12 |
| P0 | Distinguish host and agent Elements mount views | Done | Agent sandbox bind is intentionally read-only | Host confirms expected UUID mounted `rw`; sentinel present | Agent `findmnt` must not be treated as host mount evidence; run backup validation on Bumblebeam |
| P0 | Temporary Dave-OneDrive copy | Done | Remote verification commands | Restic-aware copy, check, remote restore pass | Three snapshots copied; `restic check --read-data-subset=1/10` passed; representative remote restore passed 2026-07-11 |
| P0 | Exclude repository from Ultra-Magners sync | Partial | Recheck after OneDrive reindex (2026-07-15) | Repository absent from every desktop sync scope | Set to Files On-Demand on Ultra-Magners 2026-07-14; full folder-selection exclusion still pending; reindex in progress |
| P1 | Git boundary and examples | Done | Runtime credentials remain ignored; reported exposure is unverified | Ignore audit clean; Compose examples validate | Working tree changes |
| P1 | Standardise AI harness configuration | Done | Phase 1 | Canonical `.agents/AGENTS.md`; each harness reaches it via a shim; skills shared; harness/session state kept private; global `/onboard-harness` + `/bootstrap-project` skills onboard new harnesses | `docs/adr/0002-agent-configuration-and-memory-standard.md`; `.agents/`; root `AGENTS.md`/`CLAUDE.md` shims; `~/.agents/skills/{onboard-harness,bootstrap-project}` |
| P1 | Adopt concurrent-agent workflow (minimal protocol) | Done | Enforcement tooling deferred to metric triggers | One-deployer-per-host convention recorded; ground rule + section cross-link the ADR; escalation tally started; v1 kept as deferred design | ADR 0004; `.agents/AGENTS.md`; escalation tally in ADR 0004 (currently empty); `docs/concurrent-agent-workflow-plan-v1.md` |
| P1 | Remove obsolete nested Git directories | Done | Obsolete attempts removed after backup evidence | Root Git sees only the intended repository boundary | `Home_Media/.git` and `ITS_Home_Media/` removed |
| P1 | Fix Cloudflare DDNS Compose | Done | Runtime credential rotation is deferred; local values remain ignored | `docker compose ... config --quiet` passes | Working tree changes |
| P1 | Add mount-safe backup automation | Done | Phase 1 | Read-only/missing/wrong Elements mount exits before repository creation | `docs/evidence/2026-07-11-implementation.md` |
| P1 | Pin shared Docker network identity | Done | Preserve current subnets while stabilising bridge names | Gluetun=`172.18.0.0/16` on `br-gluetun`; Traefik=`172.26.0.0/16` on `br-traefik`; host services=`172.22.0.0/24` on `br-host-svc` | `Home_Media/compose.yml`, `traefik/compose.yml`; host verification after recreation |
| P1 | Revive Bumblebeam service portal | Done | Git-managed static portal, no Elements dependency | `bumblebeam.home.arpa` returns 200; portal has no host port or Gluetun attachment | `portal/`; Traefik labels |
| P1 | Install one-shot Elements boot unit | Done | Installed and enabled on Bumblebeam | Enabled unit runs once at boot without gating services; container exits 0 with restart policy `no` | `bumblebeam-elements-boot-check.service` active (exited), 2026-07-12 |
| P1 | Canonical Traefik/Plex configuration | Done | Runtime deployment waits on backup; account credential changes remain deferred | Compose validates; canonical aliases and correct advertised IP render | `docs/evidence/2026-07-11-implementation.md` |
| P1 | Dedicated backup identity | Pending | After initial recovery safety | Two scheduled backups plus remote restore pass | Pending |
| P1 | Host/service DNS namespaces | Done | GL.iNet owns DNS/DHCP | Host records use `*.home.arpa`; wildcard/bare services use `*.svc.home.arpa` | ADR 0003; LAN-client validation 2026-07-14: bare + `*.svc.home.arpa` resolve to 192.168.1.15 and route for every service; host records bumblebeam/vodafone/glinet.home.arpa correct. Follow-ups (non-blocking): legacy per-service `*.home.arpa` aliases inconsistent (hass/prowlarr absent) — remove per ADR 0003; media upstreams time out (see Gluetun/media recovery) |
| P1 | Home Assistant recovery | Done | Proxy and UI access restored; no automation is currently configured | Direct/proxy UI login succeeds | User login verified; updated 2025.12.5 -> 2026.7.2 on 2026-07-14 (migration clean; UI 200 direct + proxy). Image still `:latest` (unpinned) |
| P1 | Plex recovery | Done | Token rotation deferred (see P0) | Ownership + playback pass | Direct and canonical proxy identity = 200; user confirms normal operation 2026-07-14 |
| P2 | n8n recovery | Partial | Recreate after security gate; webhook test | UI + webhook pass | Bare and compatibility proxy = 200; new canonical label not deployed |
| P3 | n8n local access over trusted TLS | Deferred | Trigger: heavy local n8n use, or another service needs trusted local TLS | Local login works over trusted HTTPS without disabling the Secure cookie | ADR 0005: portal links n8n to public `https://n8n.drdavient.com` (interim, done); internal TLS-everywhere (Flavour 2, ~1 day, size M) pre-scoped and deferred |
| P2 | Deploy Seerr family request interface | Partial | Seerr Compose stack and Traefik route deployed; owner must complete Plex, Sonarr, and Radarr first-run setup | `seerr.svc.home.arpa` is healthy; Plex family users can sign in and submit permission-limited requests which reach Sonarr/Radarr | Deployed 2026-07-14: container health check passes and Traefik returns the setup redirect. Use Seerr's Plex sign-in and request permissions; defer shared Bumblebeam SSO unless independent family-facing apps require a common identity layer |
| P2 | Deploy Structurizr Server | Pending | Compose/data boundary, Server URL, and authentication choice | Server is available through Traefik at a dedicated LAN hostname; Server-native authentication is enabled; runtime data and credentials remain out of Git | Replace single-workspace Structurizr Lite with a supported multi-workspace publisher; do not expose Server without authentication |
| P2 | Publish Bumblebeam and Minecraft architecture workspaces | Pending | Structurizr Server running; DSL/JSON publishing workflow | Both workspaces are independently selectable and render through Server | Keep source DSL/JSON versioned; publish through the Server API/CLI rather than editing Server data directly |
| P3 | Retire Structurizr Lite | Pending | Server acceptance, authenticated access, and both workspace checks | Lite is stopped and its port 8181 is released only after Server handover | Preserve `structurizr-lite/archive/minecraft-quark-server.dsl` until the Minecraft Server workspace is verified |
| P2 | Gluetun/media recovery | Done | Elements rw and network | Tunnel plus dependency tests pass | 2026-07-14: refreshed stale Gluetun image after its bundled OpenVPN certificate had expired, then refreshed the full media stack; tunnel healthy to Belgium, all media containers running, host-port and Traefik route checks pass (Sonarr/Radarr/Prowlarr 302; Jackett 301; Deluge 200; FlareSolverr 200). VPN-routed indexer retests: YTS/TorrentDownload validate successfully; EZTV and TorrentGalaxy currently return tracker-side rate-limit/challenge/redirect failures. |
| P3 | Retire Jackett | Done | Prowlarr is the sole active indexer manager for Sonarr/Radarr | No Sonarr/Radarr indexer points to Jackett; Jackett container, port, proxy route, portal link, and backup stop-list removed | 2026-07-14: active Sonarr/Radarr indexers are Prowlarr Torznab sources only; retained ignored `Home_Media/jackett-config/` and the pre-removal Restic snapshot `fa421c49` for rollback |
| P2 | Deploy Audiobookshelf library server | Pending | Audiobook files at `/mnt/Elements/media/audiobooks`; first-run administrator and library setup | `audiobookshelf.svc.home.arpa` is healthy; the read-only audiobook library scans and a household user can play a title | Standalone Traefik-attached Compose stack, portal link, architecture, inventory, and consistent-backup stop list prepared |
| P2 | Deploy Shelfarr audiobook request manager | Pending | Prowlarr, Deluge, Audiobookshelf, and their first-run configuration | `shelfarr.svc.home.arpa` is healthy; an authorised audiobook request downloads, imports, and triggers the Audiobookshelf scan through the Gluetun VPN boundary | HOME_MEDIA Compose, Traefik route, portal link, architecture, inventory, and consistent-backup stop list prepared |
| P3 | Migrate Elements media into canonical hierarchy | Pending | Audiobook cutover stable; an encrypted backup + restore point; no active imports, downloads, or required torrent seeding | Each affected service plays/imports from its new host path without duplicate libraries or database path rewrites | ADR 0010 defines the staged order, target layout, and per-stack changes |
| P2 | Deploy app shelf for Fire tablet sideloading | Done | Traefik-attached Compose stack; `*.svc.home.arpa` wildcard | Catalog serves APKs with the correct MIME type and Fire OS 5 badges; manager uploads land in the catalog | Deployed 2026-07-20: `apps.svc.home.arpa` renders 6 seed apps (all Fire 5 OK) and serves `application/vnd.android.package-archive`; `appfiles.svc.home.arpa` returns 200; seed set SHA-256-verified via `fetch-seed-apks.sh`. ADR 0011; runbook `fire-tablet-sideload.md`. Follow-up: owner to add owned paid titles (Minecraft PE, Angry Birds) via an authorised source; change Filebrowser admin password |
| P2 | Investigate Home Assistant Bluetooth scanner errors | Pending | Assess `hci0` integration/device state | Scanner starts/stops without repeated errors, or Bluetooth is deliberately disabled | 2026.7.2 logs (2026-07-14) give explicit cause: container lacks NET_ADMIN/NET_RAW capabilities for Bluetooth management; `hci0` force-stop errors persist. Fix candidate: add those caps, or disable onboard Bluetooth. Unrelated to Zigbee/Pomodoro |
| P2 | Restore Home Assistant Chromecast connectivity | Pending | Living Room TV reachability/discovery | Cast integration connects without retries | `pychromecast` retry observed for `192.168.1.202:8009` |
| P2 | Daily timers and retention | Pending | Host-level Elements `rw` check; remote destination explicitly authorised | Timer succeeds; retention dry-run matches 7/4/12 | Agent bind is read-only by design; timer units not installed |
| P2 | Monthly integrity/restore checks | Pending | Scheduled backup stable | Rotating subset and both restores pass | Pending |
| P3 | Append-only/object-lock or offline copy | Pending | Backend decision | Host deletion credentials cannot remove protected copy | Pending |
| P3 | Zigbee stack (Zigbee2MQTT + Mosquitto) for local sensors | Done | SONOFF ZBDongle-E on Bumblebeam | Coordinator healthy; a Zigbee device pairs and reports | ADR 0006; `zigbee/`; EmberZNet 7.4.4 / Z2M 2.12.1; DJT11LM paired 2026-07-14 |
| P3 | Pomodoro cube: DJT11LM orientation validation | Done | Zigbee stack | Six faces produce stable, distinguishable sensor signatures | ADR 0006; 2026-07-14: all six faces tested (UP=−Z, 5=−X, 10=−Y, 25=+X, 50=+Y, down=+Z), each a distinct orthogonal gravity vector (±~1000 vs ±tens), stable and cleanly separable |
| P3 | Pomodoro cube: focus routine | Done | — | Numbered face up -> focus ON (Android DND + Windows DND); UP/face-down -> OFF; no timers | ADR 0008. Verified end-to-end on both platforms 2026-07-14: cube -> focus/state -> Android DND and Windows DND all track the flips. Win11 2026 build has no registry/API DND lever (legacy toasts + Focus Assist WNF dead; DND off/on registry dumps byte-identical — live state is shell-memory only), so the agent drives the real Notification Center toggle (Win+N; UIA focused-element read; Enter), state-checked so replays never invert, repeats deduped. Latency tuned: HA debounce removed (DJT11LM firmware settle suffices), UIA tree search bypassed. User confirms "nice and fast" |
| P3 | Add Zigbee runtime to encrypted backup scope | Pending | `zigbee/zigbee2mqtt/data/` holds the network key + coordinator backup | Losing it forces re-pairing; must be in Restic scope, not Git | Update `docs/inventory.md` |
| P2 | Remote SSH access via Tailscale | Done | Native install (not Compose) so the rescue line survives Docker failures; no public port/DNS/Traefik exposure | Bumblebeam reachable over the tailnet; sshd not internet-exposed | ADR 0009; `docs/runbooks/remote-access.md`; installed 2026-07-15, `bumblebeam` = 100.85.155.36; owner verified remote SSH from an enrolled client 2026-07-15 |
| P3 | Read-only video browse endpoint | Done | Serves the legacy `/mnt/Elements/Video` path; update the volume when the ADR 0010 migration reaches video | `video.svc.home.arpa` serves the library read-only with Range/seek | Nginx stack `video/` on traefik-net; deployed 2026-07-20; found running but untracked during the 2026-07-21 commit sweep |
| P3 | Adopt OpenSpeedTest as a tracked stack | Partial | Recreate under Compose (backup gate first, per ADR 0004): `docker rm -f openspeedtest && docker compose -f openspeedtest/compose.yml up -d` | LAN speed test reachable directly on `:3000` and via `speedtest.svc.home.arpa`; comparing the two isolates Traefik overhead from link problems | Found as an orphaned `docker run` container (bridge, `restart=no`) by preflight 2026-07-21; stack `openspeedtest/` prepared 2026-07-22, live container not yet recreated under Compose |
| P1 | Layered workflow enforcement (preflight + commit gate) | Done | Doctrine-only delivery chosen after measurement (0/3 old wording → 3/3 imperative); SessionStart hook deliberately deferred behind escalation triggers; wire `core.hooksPath` per clone | `/preflight` runs first in every session and logs to `runs.log`; a wired clone refuses commits with a broken compose render or a pattern-matching secret | ADR 0012 + 2026-07-22 amendment (monitoring + 4-rung escalation ladder: cron → hook → eval harness → SkillOpt); `.agents/skills/preflight/`; `ops/git-hooks/pre-commit`; drift event in ADR 0004 tally. Review `runs.log` when working this register |
